### sponsor

i built this simple thing with help from the online resources because there was no easily findable and updated and pretty way to redirect github sponsor webhook -> discord webhook. now there is :3

it uses the discord v2 component system cus its rly pretty i thinks

### usage

i dont expect anyone to use this buuuuut, all u need is 2 env variables and build with node on like vercel:

- `WEBHOOK_SECRET`
- `DISCORD_WEBHOOK_URL`
